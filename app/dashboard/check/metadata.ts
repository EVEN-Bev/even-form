import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'EVEN Admin | Database Structure Check',
  description: 'Diagnostic tool for checking EVEN database structure and tables.',
}
