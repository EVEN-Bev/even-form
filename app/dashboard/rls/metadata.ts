import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'EVEN Admin | RLS Check',
  description: 'Diagnostic tool for checking Row Level Security settings in the EVEN database.',
}
