import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'EVEN Admin | Reset Password',
  description: 'Reset your password for the EVEN admin dashboard to regain access to your account.',
}
